package main

import "strings"

func main() {

}

func addBinary(a string, b string) string {
	var s strings.Builder
	carry := 0
	lenA := len(a)
	lenB := len(b)

	for i := min(lenA, lenB) - 1; i > 0; i-- {

	}
}
